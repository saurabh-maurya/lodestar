# Headless WordPress (Resources → Blog). Omit to render the built-in fallback posts.
WORDPRESS_API_URL=https://cms.internal.lodestar.guru

# Google Sheets lead capture. Server-side only — never prefix with NEXT_PUBLIC_.
GOOGLE_SHEETS_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
